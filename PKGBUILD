# Script generated with Bloom
pkgdesc="ROS - hector_worldmodel_geotiff_plugins contains a plugin to visualize worldmodel objects (e.g. victims in a Search and Rescue scenario) in a geotiff map generated by <a href="http://wiki.ros.org/hector_geotiff">hector_geotiff</a>."
url='http://ros.org/wiki/hector_worldmodel_geotiff_plugins'

pkgname='ros-kinetic-hector-worldmodel-geotiff-plugins'
pkgver='0.3.4_1'
pkgrel=1
arch=('any')
license=('BSD'
)

makedepends=('ros-kinetic-catkin'
'ros-kinetic-hector-geotiff'
'ros-kinetic-hector-worldmodel-msgs'
)

depends=('ros-kinetic-hector-geotiff'
'ros-kinetic-hector-worldmodel-msgs'
)

conflicts=()
replaces=()

_dir=hector_worldmodel_geotiff_plugins
source=()
md5sums=()

prepare() {
    cp -R $startdir/hector_worldmodel_geotiff_plugins $srcdir/hector_worldmodel_geotiff_plugins
}

build() {
  # Use ROS environment variables
  source /usr/share/ros-build-tools/clear-ros-env.sh
  [ -f /opt/ros/kinetic/setup.bash ] && source /opt/ros/kinetic/setup.bash

  # Create build directory
  [ -d ${srcdir}/build ] || mkdir ${srcdir}/build
  cd ${srcdir}/build

  # Fix Python2/Python3 conflicts
  /usr/share/ros-build-tools/fix-python-scripts.sh -v 2 ${srcdir}/${_dir}

  # Build project
  cmake ${srcdir}/${_dir} \
        -DCMAKE_BUILD_TYPE=Release \
        -DCATKIN_BUILD_BINARY_PACKAGE=ON \
        -DCMAKE_INSTALL_PREFIX=/opt/ros/kinetic \
        -DPYTHON_EXECUTABLE=/usr/bin/python2 \
        -DPYTHON_INCLUDE_DIR=/usr/include/python2.7 \
        -DPYTHON_LIBRARY=/usr/lib/libpython2.7.so \
        -DPYTHON_BASENAME=-python2.7 \
        -DSETUPTOOLS_DEB_LAYOUT=OFF
  make
}

package() {
  cd "${srcdir}/build"
  make DESTDIR="${pkgdir}/" install
}

